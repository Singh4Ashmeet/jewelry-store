import { StaticPage } from "@/components/common/static-page";

export default function Page() {
  return <StaticPage title="Contact" copy="For styling, gifting, bridal consultation, or order support, this page is ready for Resend-backed contact workflows." />;
}
