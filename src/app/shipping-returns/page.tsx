import { StaticPage } from "@/components/common/static-page";

export default function Page() {
  return <StaticPage title="Shipping & Returns" copy="Insured shipping and return policy content can be finalized here once operational rules are confirmed." />;
}
