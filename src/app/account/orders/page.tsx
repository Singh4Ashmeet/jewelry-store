import { StaticPage } from "@/components/common/static-page";

export default function Page() {
  return <StaticPage title="Your Orders" copy="Customer order history UI shell with tracking, invoices, and reorder actions ready for database wiring." />;
}
